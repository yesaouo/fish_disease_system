# Backend environment variables (.env at repo root)
# Copy this file to ".env" and adjust as needed.

# DATA_ROOT: absolute path to datasets root. Defaults to <repo>/data
# On Windows, use full path, e.g.:
# DATA_ROOT=C:\workspace\annotation_web\data
# On Linux/macOS, e.g.:
# DATA_ROOT=/srv/fish/data

# Cache windows (seconds)
STATS_CACHE_SECONDS=10
DATASET_CACHE_SECONDS=10
CLASSES_CACHE_SECONDS=10

# Audit log filename under DATA_ROOT
AUDIT_LOG_FILENAME=audit_log.jsonl

# Idle backup settings
IDLE_BACKUP_ENABLED=true
IDLE_BACKUP_SECONDS=21600
IDLE_CHECK_INTERVAL_SECONDS=60
BACKUP_DIRNAME=backup

# If deploying under a subpath, set root path prefix (no trailing slash)
# Example: /fish
ROOT_PATH=

# Auth: file under DATA_ROOT containing one API key per line
# Default: auth_keys.txt
# AUTH_KEYS_FILENAME=auth_keys.txt

# Advanced: point to a custom env file path instead of <repo>/.env
# ENV_FILE=
